#include "../include/so_long.h"

/* 
int ft_player(t_map_data *map_data)
{
    int i;
    int j;

    while (map_data->matrice[i] != NULL)
    {
        if(map_data->matrice[i][j])
    }
    return;
} */